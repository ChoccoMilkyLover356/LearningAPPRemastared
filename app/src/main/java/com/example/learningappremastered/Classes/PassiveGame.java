package com.example.learningappremastered.Classes;

public class PassiveGame extends Game {
    public PassiveGame(int totalPlayTime, int playTimeToday, boolean visible, String name, int resource) {
        super(totalPlayTime, playTimeToday, visible, name, resource);
    }
}
