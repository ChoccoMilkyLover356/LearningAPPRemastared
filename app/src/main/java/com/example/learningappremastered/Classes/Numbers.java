package com.example.learningappremastered.Classes;

public class Numbers extends ActiveGame {
    public Numbers(int totalPlayTime, int playTimeToday, boolean visible, String name, int resources, int points) {
        super(totalPlayTime, playTimeToday, visible, name, resources, points);
    }
}
